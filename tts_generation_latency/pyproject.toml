[project]
name = "tts-websocket-latency"
version = "0.1.0"
description = "Stream text to Deepgram TTS websocket and record timing metrics for latency analysis"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "websockets==12.0",
    "click>=8.0",
]
